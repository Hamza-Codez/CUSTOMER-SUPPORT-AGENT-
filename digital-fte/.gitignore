# python
__pycache__/
*.pyc
.venv/
# Every .env variant is ignored EXCEPT the committed template. A real key
# pasted into .env.example would otherwise be committed — .env.example is
# tracked, .env is not.
.env
.env.*
!.env.example

# node / next
node_modules/
.next/
.env.local
.pytest_cache/
